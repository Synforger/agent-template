# Third-Party Notices

This repository ships only its own shell / Python scripts and Markdown
templates — it bundles no third-party code and installs no runtime
dependencies. The tools it invokes (git, bash, python3, jq where noted)
are user-installed system binaries, not shipped in this tree.

If a bundled dependency is ever added, enumerate it here (package /
version / license / source) in the same format as the other Synforger
repositories.
